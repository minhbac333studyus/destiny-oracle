import { Routes } from '@angular/router';
import { ReflectionChamberPageComponent } from './reflection-chamber-page.component';

export const REFLECTION_ROUTES: Routes = [
  { path: '', component: ReflectionChamberPageComponent },
];
