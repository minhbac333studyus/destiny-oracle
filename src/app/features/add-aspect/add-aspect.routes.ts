import { Routes } from '@angular/router';
import { AddAspectPageComponent } from './add-aspect-page.component';

export const ADD_ASPECT_ROUTES: Routes = [
  { path: '', component: AddAspectPageComponent },
];
