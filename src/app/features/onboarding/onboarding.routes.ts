import { Routes } from '@angular/router';
import { OnboardingShellComponent } from './onboarding-shell.component';

export const ONBOARDING_ROUTES: Routes = [
  { path: '', component: OnboardingShellComponent },
];
